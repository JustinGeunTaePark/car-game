/**
 * Game Options contains the different options a user can choose on the start menu.
 *
 */
public enum GameOptions {
	;
	/**
	 * Difficulty level
	 */
	public static enum Level {
		Easy,
		Medium,
		Hard,
		VeryHard
	}
	/**
	 * Game mode
	 */
	public static enum Mode {
		Standard,
		Story
	}
}
